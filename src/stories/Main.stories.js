import React from 'react'
import Main from '../containers/Main'

export default {
  component: Main,
  title: 'Containers/Main/Main',
}

const Template = () => (
  <Main />
)

export const Default = Template.bind({})

Default.args = {}
