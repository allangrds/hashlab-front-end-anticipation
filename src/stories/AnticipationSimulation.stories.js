import React from 'react'
import AnticipationSimulation from '../containers/Main/AnticipationSimulation'

export default {
  component: AnticipationSimulation,
  title: 'Containers/Main/AnticipationSimulation',
}

const Template = () => (
  <AnticipationSimulation />
)

export const Default = Template.bind({})

Default.args = {}
